export enum routes {
  HOME = "/",
  CLASSE = "/classe",
  CLASSES = "/classes",
  STUDENTS = "/students",
  LOGIN = "/login",
  REGISTER_STUDENT = "/registerStudent",
  REGISTER_TEACHER = "/registerTeacher",
}
